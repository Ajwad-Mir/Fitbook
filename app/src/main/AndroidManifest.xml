<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.fitbook">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Fitbook">
        <activity android:name=".leaderboard.LeaderboardHome"></activity>
        <activity android:name=".workout.WorkoutFinishActivity" />
        <activity android:name=".workout.WorkoutStartActivity" />
        <activity android:name=".workout.WorkoutFullListActivity" />
        <activity android:name=".workout.WorkoutAddExerciseActivity" />
        <activity android:name=".workout.WorkoutCreateNewExerciseListActivity" />
        <activity android:name=".workout.WorkoutHomeListActivity" />
        <activity android:name=".diets.DietsHomeListActivity" />
        <activity android:name=".shoes.ShoesAddListActivity" />
        <activity android:name=".shoes.ShoesHomeListActivity" />
        <activity
            android:name=".SettingsActivity"
            android:label="@string/title_activity_settings" />
        <activity android:name=".musicplayer.MusicPlayerMainPlayerActivity" />
        <activity android:name=".musicplayer.MusicPlayerSongList" />
        <activity android:name=".AboutUsActivity" />
        <activity android:name=".Homepage" />
        <activity android:name=".users.LogInActivity" />
        <activity android:name=".users.RegisterActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>